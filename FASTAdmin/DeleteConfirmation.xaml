<Window x:Class="FASTAdmin.DeleteConfirmation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FASTAdmin"
        mc:Ignorable="d" FontSize="18"
        Title="Анхааруулга" Height="150" Width="350" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text=" Та устгахдаа итгэлтэй байна уу?" 
            FontSize="18"
            TextWrapping="Wrap"
            TextAlignment="Center"/>

        <Rectangle Grid.Row="1" Fill="#f0f0f0" />
        <StackPanel
            Grid.Row="1" 
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            VerticalAlignment="Center">
            <Button x:Name="agreeButton" Click="agreeButton_Click" Width="60" Height="27">Тийм</Button>
            <Button x:Name="declineButton" Click="declineButton_Click" Width="60" Height="27" Margin="20 0">Үгүй</Button>
        </StackPanel>

    </Grid>
</Window>
